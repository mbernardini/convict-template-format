{
  "name": "convict-template-format",
  "version": "0.1.1",
  "description": "Adds a template format for mozilla/node-convict package",
  "main": "dist/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "tsc",
    "test": "jest --env node --ci"
  },
  "author": "Marc Bernardini",
  "license": "MIT",
  "devDependencies": {
    "@types/convict": "^6.1.1",
    "@types/jest": "^29.2.4",
    "jest": "^29.3.1",
    "ts-jest": "^29.0.3",
    "typescript": "^4.9.4"
  },
  "dependencies": {
    "convict": "^6.2.3"
  }
}
